# secure-bootstrap-transport.startup-handshake

Feature folder for `secure-bootstrap-transport.startup-handshake` under root `secure-bootstrap-transport`. Canonical metadata lives in `workflow.toml`.
