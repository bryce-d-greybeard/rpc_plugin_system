# secure-bootstrap-transport.windows-transport

Feature folder for `secure-bootstrap-transport.windows-transport` under root `secure-bootstrap-transport`. Canonical metadata lives in `workflow.toml`.
