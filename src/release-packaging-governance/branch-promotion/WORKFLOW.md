# release-packaging-governance.branch-promotion

Feature folder for `release-packaging-governance.branch-promotion` under root `release-packaging-governance`. Canonical metadata lives in `workflow.toml`.
