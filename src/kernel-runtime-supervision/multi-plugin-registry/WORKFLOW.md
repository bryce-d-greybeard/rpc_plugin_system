# kernel-runtime-supervision.multi-plugin-registry

Feature folder for `kernel-runtime-supervision.multi-plugin-registry` under root `kernel-runtime-supervision`. Canonical metadata lives in `workflow.toml`.
