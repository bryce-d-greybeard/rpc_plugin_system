# kernel-runtime-supervision.daemon-entrypoint

Feature folder for `kernel-runtime-supervision.daemon-entrypoint` under root `kernel-runtime-supervision`. Canonical metadata lives in `workflow.toml`.
