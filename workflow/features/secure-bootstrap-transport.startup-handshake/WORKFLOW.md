# secure-bootstrap-transport.startup-handshake

Feature workflow record for `secure-bootstrap-transport.startup-handshake` under root `secure-bootstrap-transport`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/internal/auth`.
