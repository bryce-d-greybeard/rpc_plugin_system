# kernel-runtime-supervision.daemon-entrypoint

Feature workflow record for `kernel-runtime-supervision.daemon-entrypoint` under root `kernel-runtime-supervision`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/cmd/rpcplugind`.
