# plugin-contracts.api-surface

Feature workflow record for `plugin-contracts.api-surface` under root `plugin-contracts`. Canonical metadata lives in `../../workflow.toml`; source lives under `docs`.
