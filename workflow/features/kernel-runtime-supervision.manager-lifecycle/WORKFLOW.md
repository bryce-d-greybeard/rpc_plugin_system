# kernel-runtime-supervision.manager-lifecycle

Feature workflow record for `kernel-runtime-supervision.manager-lifecycle` under root `kernel-runtime-supervision`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/kernel-runtime-supervision/manager-lifecycle`.
