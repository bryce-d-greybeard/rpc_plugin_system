# kernel-runtime-supervision.manager-lifecycle

Feature folder for `kernel-runtime-supervision.manager-lifecycle` under root `kernel-runtime-supervision`. Canonical metadata lives in `workflow.toml`.
