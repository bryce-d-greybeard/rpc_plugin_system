# plugin-contracts.standard-v0

Feature workflow record for `plugin-contracts.standard-v0` under root `plugin-contracts`. Canonical metadata lives in `../../workflow.toml`; source lives under `docs`.
