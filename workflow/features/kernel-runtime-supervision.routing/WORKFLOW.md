# kernel-runtime-supervision.routing

Feature workflow record for `kernel-runtime-supervision.routing` under root `kernel-runtime-supervision`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/kernel-runtime-supervision/routing`.
