# secure-bootstrap-transport.session-identity

Feature workflow record for `secure-bootstrap-transport.session-identity` under root `secure-bootstrap-transport`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/secure-bootstrap-transport/session-identity`.
