# kernel-runtime-supervision.multi-plugin-registry

Feature workflow record for `kernel-runtime-supervision.multi-plugin-registry` under root `kernel-runtime-supervision`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/internal/kernel`.
