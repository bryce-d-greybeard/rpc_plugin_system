# release-packaging-governance.go-build-deps

Feature workflow record for `release-packaging-governance.go-build-deps` under root `release-packaging-governance`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/release-packaging-governance/go-build-deps`.
