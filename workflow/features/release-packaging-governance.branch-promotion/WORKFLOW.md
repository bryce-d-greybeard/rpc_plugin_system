# release-packaging-governance.branch-promotion

Feature workflow record for `release-packaging-governance.branch-promotion` under root `release-packaging-governance`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/release-packaging-governance/branch-promotion`.
