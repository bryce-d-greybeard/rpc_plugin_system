# secure-bootstrap-transport.secure-framing

Feature workflow record for `secure-bootstrap-transport.secure-framing` under root `secure-bootstrap-transport`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/secure-bootstrap-transport/secure-framing`.
