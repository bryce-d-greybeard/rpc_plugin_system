# plugin-contracts.compatibility-versioning

Feature workflow record for `plugin-contracts.compatibility-versioning` under root `plugin-contracts`. Canonical metadata lives in `../../workflow.toml`; source lives under `docs`.
