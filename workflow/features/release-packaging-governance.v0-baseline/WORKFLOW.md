# release-packaging-governance.v0-baseline

Feature workflow record for `release-packaging-governance.v0-baseline` under root `release-packaging-governance`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/release-packaging-governance/v0-baseline`.
