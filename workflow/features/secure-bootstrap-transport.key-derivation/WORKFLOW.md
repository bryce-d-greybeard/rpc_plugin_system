# secure-bootstrap-transport.key-derivation

Feature workflow record for `secure-bootstrap-transport.key-derivation` under root `secure-bootstrap-transport`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/internal/auth`.
