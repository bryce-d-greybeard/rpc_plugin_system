# control-plane-ops.admin-rpc

Feature workflow record for `control-plane-ops.admin-rpc` under root `control-plane-ops`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/control-plane-ops/admin-rpc`.
