# plugin-authoring-sdk.logging

Feature workflow record for `plugin-authoring-sdk.logging` under root `plugin-authoring-sdk`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/pkg/plugin`.
