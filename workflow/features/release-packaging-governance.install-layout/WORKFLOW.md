# release-packaging-governance.install-layout

Feature workflow record for `release-packaging-governance.install-layout` under root `release-packaging-governance`. Canonical metadata lives in `../../workflow.toml`; source lives under `docs`.
