# release-packaging-governance.install-layout

Feature folder for `release-packaging-governance.install-layout` under root `release-packaging-governance`. Canonical metadata lives in `workflow.toml`.
