# control-plane-ops.event-log-write

Feature workflow record for `control-plane-ops.event-log-write` under root `control-plane-ops`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/internal/eventlog`.
