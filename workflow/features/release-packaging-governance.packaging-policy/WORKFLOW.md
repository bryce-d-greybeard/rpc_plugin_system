# release-packaging-governance.packaging-policy

Feature workflow record for `release-packaging-governance.packaging-policy` under root `release-packaging-governance`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/release-packaging-governance/packaging-policy`.
