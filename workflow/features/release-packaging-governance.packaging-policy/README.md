# release-packaging-governance.packaging-policy

Feature folder for `release-packaging-governance.packaging-policy` under root `release-packaging-governance`. Canonical metadata lives in `workflow.toml`.
