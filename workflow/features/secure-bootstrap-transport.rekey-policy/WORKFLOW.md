# secure-bootstrap-transport.rekey-policy

Feature workflow record for `secure-bootstrap-transport.rekey-policy` under root `secure-bootstrap-transport`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/secure-bootstrap-transport/rekey-policy`.
