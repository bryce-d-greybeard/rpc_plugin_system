# plugin-contracts.abi-startup

Feature workflow record for `plugin-contracts.abi-startup` under root `plugin-contracts`. Canonical metadata lives in `../../workflow.toml`; source lives under `docs`.
