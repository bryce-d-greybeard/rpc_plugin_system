# kernel-runtime-supervision.failure-isolation

Feature folder for `kernel-runtime-supervision.failure-isolation` under root `kernel-runtime-supervision`. Canonical metadata lives in `workflow.toml`.
