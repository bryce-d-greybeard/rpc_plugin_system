# control-plane-ops.cli-argument-model

Feature workflow record for `control-plane-ops.cli-argument-model` under root `control-plane-ops`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/control-plane-ops/cli-argument-model`.
