# kernel-runtime-supervision.peercred-runtime

Feature workflow record for `kernel-runtime-supervision.peercred-runtime` under root `kernel-runtime-supervision`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/internal/runtime`.
