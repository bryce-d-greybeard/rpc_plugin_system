# control-plane-ops.local-observability

Feature workflow record for `control-plane-ops.local-observability` under root `control-plane-ops`. Canonical metadata lives in `../../workflow.toml`; source lives under `src/internal/eventlog`.
