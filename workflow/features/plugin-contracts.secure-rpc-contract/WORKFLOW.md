# plugin-contracts.secure-rpc-contract

Feature workflow record for `plugin-contracts.secure-rpc-contract` under root `plugin-contracts`. Canonical metadata lives in `../../workflow.toml`; source lives under `docs`.
