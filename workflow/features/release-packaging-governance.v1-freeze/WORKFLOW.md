# release-packaging-governance.v1-freeze

Feature workflow record for `release-packaging-governance.v1-freeze` under root `release-packaging-governance`. Canonical metadata lives in `../../workflow.toml`; source lives under `docs`.
